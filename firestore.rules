rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra global: Permite leitura e escrita em toda a base de dados
    // para qualquer utilizador que esteja autenticado.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
