rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permite que qualquer utilizador autenticado leia e escreva em todos os documentos.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
