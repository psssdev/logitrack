
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isUserAuthenticated() {
      return request.auth != null;
    }

    // Allow a user to read and write ONLY their own profile document.
    match /users/{userId} {
      allow read, write: if isUserAuthenticated() && request.auth.uid == userId;
    }

    // Helper function to get the companyId from the user's auth token
    function getUserCompanyId() {
      return request.auth.token.companyId;
    }
    
    // This single rule protects all subcollections within a company document
    match /companies/{companyId}/{document=**} {
      allow read, write: if isUserAuthenticated() && getUserCompanyId() == companyId;
    }
  }
}
